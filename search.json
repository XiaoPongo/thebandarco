---
layout: none
permalink: /search.json
---
[
  {% assign all_pages = site.pages | concat: site.posts %}
  {% assign filtered_pages = all_pages | reject: "url", "/tags/" | reject: "url", "/categories/" | reject: "url", "/journal/" %}
  {% for page in filtered_pages %}
    {% unless page.url contains '/tags/' or page.url contains '/categories/' or page.url contains '/journal/' or page.search == false %}
      {
        "title": {{ page.title | jsonify }},
        "url": {{ page.url | jsonify }},
        "content": {{ page.content | strip_html | strip_newlines | jsonify }}
      }{% unless forloop.last %},{% endunless %}
    {% endunless %}
  {% endfor %}
]
